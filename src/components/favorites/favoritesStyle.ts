import { styled } from '@mui/material/styles';

export const FavoritesWrapper = styled('div')(({theme}) => ({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  fontSize: '2em',
  fontWeight: 'bolder',
  textAlign: 'center',
  marginTop: '10%',
}));